#include <iostream>
using namespace std;

int main()
{
    int a;
    float b = 0;
    cout<<"enter the number of late days: ";
    cin>>a;
    
    if(a<=5)   b = a*0.50;
    else if (a>=6 && a<=10) b= (5 * 0.50) + ((a - 5) * 1);
    
    else if(a>10 && a<=30)  b= (5 * 0.50) + (5 * 1) + ((a - 10) * 5);
    
    else{ cout<<" MEMBERSHIP CANCELLED";}
    
    cout<<"fine imposed is Rs "<<b;
}
